/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com', 'another-domain.com', 'yet-another-domain.com','assets.aceternity.com'],
    },
  };
  
  export default nextConfig;