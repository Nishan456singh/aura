"use client"

import React from 'react'

type EventFormProps = {
    userId: string
    type: "Create" | "Update"
    eventId?: string
  }
  
  const Addnewitems = ({ userId, type, eventId }: EventFormProps) => {
  return (
    <div>
      
    </div>
  )
}

export default Addnewitems